[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "rust_pyo3_demo"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "rust_pyo3_demo"
features = ["pyo3/extension-module"]
